version: '3.8'

services:
  app:
    environment:
      - NODE_ENV=development
      - APP_ENV=development
      - DEV_MODE=true
    # In development mode, OTP verification codes are displayed directly via in-site notifications/dialogs
    # and Authentica live SMS dispatch is safely bypassed.
